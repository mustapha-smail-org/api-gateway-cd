apiVersion: delivery.citypulse.io/v1
kind: ServiceDeployment
metadata:
  name: api-gateway
  owner: data-platform
spec:
  provider: render
  imageRepository: ghcr.io/mustapha-smail-org/api-gateway
  health:
    path: /actuator/health/readiness
    timeoutSeconds: 300
